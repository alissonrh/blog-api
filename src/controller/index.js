const loginCrontroller = require('./login.controller');
const userCrontroller = require('./user.controller');
const categoryController = require('./categories.controller');
const postController = require('./post.controller');

module.exports = {
  loginCrontroller,
  userCrontroller,
  categoryController,
  postController,
};